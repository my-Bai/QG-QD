# CSS样式基础  

> *CSS（Cascading Style Sheets）指层叠样式表，HTML用于描述网页的内容，而CSS用于定义网页的样式，包括针对不同设备和屏幕尺寸的设计和布局。*  

> *样式定义通常保存在外部.css后缀的文件，即外部样式表文件中，通过导入该文件，只需更改文件内容即可更改整个网页的外观。*  

## CSS规则集  

CSS规则集是CSS样式表的基本组成单元，用于定义HTML元素的显示样式，由**选择器**和**声明块**两部分组成。  

